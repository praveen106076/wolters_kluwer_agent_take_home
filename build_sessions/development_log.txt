I have an AI engineering take-home challenge. I need to build an autonomous research agent.

The Constraints:

No agent frameworks allowed (no LangChain, LangGraph, etc.).

Must use a ReAct loop (Thought -> Action -> Observation -> Final Answer).

Must be observable (CLI-based preferred).

Must be resilient (handle tool errors).

Can you help me brainstorm the architecture? I want to keep it simple, modular, and maintainable. Let's start with a high-level file structure.

To keep the architecture simple, modular, and maintainable while adhering to your constraints, I recommend a flat, decoupled structure. This allows you to swap out tools or modify the prompt without touching the agent's core execution loop.

Here is the recommended high-level file structure for your project:

Recommended Project Structure
Plaintext
research_agent/
├── build_sessions/       # Folder for raw development/debugging logs
├── .env                  # Environment variables (API keys)
├── .gitignore            # Exclude .env and __pycache__
├── main.py               # The core ReAct loop engine
├── tools.py              # Modular definitions for search_web, read_file, etc.
├── prompts.py            # Centralized system prompt and formatting instructions
├── evaluator.py          # Automated harness to test agent capabilities
├── run.py                # Simple entry point for a single task demo
└── requirements.txt      # Project dependencies (openai, python-dotenv)
Architectural Rationale
main.py (The Engine): This is the only place where the while loop resides. It maintains the history list (the agent's memory) and manages the interaction with OpenAI. By keeping it small, you ensure it is easy to read and debug.

tools.py (The Extensibility): By defining tools as standalone functions, you can easily add more (e.g., calculate_math, get_current_date) later. Each tool should handle its own internal errors and return a clean string observation.

prompts.py (The Behavioral Control): Keeping the prompt in a separate file makes it easier to iterate on the agent’s "personality" or "reasoning style" without cluttering your logic code.

evaluator.py (The Validation): By separating this from the run.py script, you ensure that you can run automated quality checks on your agent’s reasoning without manually triggering tasks every time you make a change.

How this meets your constraints:
No Frameworks: You are using only the openai SDK and standard Python libraries.

ReAct Loop: The loop in main.py will explicitly check for "Action" and "Final Answer" strings to drive state transitions.

Observable: By printing the "Thought," "Action," and "Observation" to the console in main.py, you satisfy the visibility requirement without needing a UI.

Resilient: If tools.py catches an exception, it should return a descriptive string (e.g., "Error: File not found") as the result, which main.py will append as an "Observation." This forces the agent to see the error and correct its course.

Does this structure align with your vision for the project, or would you like to refine the tool-handling logic before moving to the implementation phase?

That structure looks good. Let's build main.py. I want a while loop that maintains a history list. It should send the conversation to OpenAI, parse the output for 'Action' vs 'Final Answer', and if it finds an 'Action', execute the corresponding tool. Please provide the initial implementation for this loop logic, focusing on clarity and state management.

That is a solid plan. Keeping the loop logic decoupled from the tool execution is exactly the right move. This ensures main.py acts as a pure "orchestrator."

Here is the initial implementation for main.py. I’ve included a max_steps limit to prevent infinite loops and implemented a basic parser to look for the "Action" and "Final Answer" keywords.
main.py
29 Jun, 3:46 pm

Why this design works:
The Parsing (re.search): Using regex is much more reliable than simple string splitting for finding the Action and Action Input tags in the LLM's raw text.

The History State: We append both the Assistant's reasoning (for transparency) and the subsequent Observation (from the tool) to the history, which provides the model with the necessary context to make its next thought.

Observability: The print statements act as your live "trace." If this were a production system, you'd replace these with a logging library, but for a take-home, this is perfect for debugging.

How does this loop structure look to you? Are you ready to define the tools.py logic to go with this execution engine?

Now let's build the tools.py file. I need a search_web and a read_file tool. For now, they can be stubs. Also, how should I handle error cases? If a tool fails, I want the agent to 'see' the error message so it can try again. How should I integrate this into the loop logic?

To build tools.py, we need to focus on a robust "Dispatcher" pattern. This ensures that main.py doesn't need to know how tools work, only that it can pass a name and an input to execute_tool.

Handling Errors
The best way to handle errors is to wrap the execution in a try...except block inside the tool dispatcher. Instead of letting the program crash, the tool should return a clear, descriptive string explaining exactly what went wrong. This string becomes an "Observation," which the LLM then receives and uses to correct its reasoning (e.g., "Oh, the file path was wrong; I'll try a different one").

Here is the implementation:
tools.py
29 Jun, 3:47 pm

How this integrates into your loop:
The Catch: In tools.py, the execute_tool function catches any exception that happens inside a tool and returns it as a string.

The Observation: In main.py, the returned string (whether it's valid data or an error message) is assigned to the observation variable.

The Loop: Because you are appending that observation to the history as a user role message, the LLM receives it in the next request.

Example of agent resilience:

Agent: "I will read missing_file.txt."

Tool: Returns "Tool Execution Error: File 'missing_file.txt' not found."

Agent (next step): "I tried to read the file, but it failed. I should search_web for the correct file path instead."

This turns a potential crash into a "learning moment" for the agent. Does this integration logic make sense for your implementation?

The prompt is critical here. If the LLM doesn't follow the format, the loop breaks. Let's design a prompts.py that enforces the Thought/Action/Action Input/Observation cycle. What specific instructions should I include to ensure the agent is concise and doesn't get stuck in a loop?

To ensure your agent follows the ReAct format strictly and avoids infinite loops, your system prompt needs to be directive, constraint-heavy, and structured.

The core of the issue with LLMs is that they prefer to be "helpful" conversationalists. You must force them into the role of a "logical execution engine."

Recommended prompts.py
prompts.py
29 Jun, 3:47 pm
Why this specific prompt works:
Strict Termination: By explicitly telling the model "If you have enough information... you MUST output Final Answer," you reduce the tendency to keep "thinking" even when the job is done.

Error Handling Guidance: Instruction like "If a tool fails, analyze the error... and try a different tool" turns an error message from a failure into a deliberate step in the reasoning chain.

Role Definition: By calling the agent a "logical execution engine" rather than a "helpful assistant," you prevent the model from adding conversational fluff like "Sure! I'd be happy to help you with that."

Formatting Constraints: Because your main.py is parsing based on those exact keywords ("Thought:", "Action:", "Final Answer:"), the prompt acts as a contract.

Pro-Tip for your Development Log:
When you record your build session, you can mention:

"I iterated on the system prompt several times. I found that if I didn't explicitly instruct the model to analyze tool errors, it would occasionally loop on the same failed read_file call. Adding the 'Operational Constraints' section fixed that by forcing it to switch strategies upon seeing a 'Tool Execution Error'."

This demonstrates that you understand the "why" behind the prompt, which is a key part of AI engineering judgment. Does this structure feel robust enough for the tasks you have planned?

I need to build an evaluation harness (evaluator.py). I need to test if the agent can handle multi-step reasoning. Let's design a script that runs a few test cases and reports a pass/fail summary. How can I ensure the agent is evaluated fairly based on its output?

To build an effective evaluation harness for your agent, you need to treat it as a deterministic validation gate. Since your agent is CLI-based and produces logs (observability), your evaluator should programmatically verify the output.

For this "simple harness" requirement, I recommend a script that performs two types of checks:

Format Check: Did it produce a Final Answer?

Content Check: Does the output contain expected keywords or fulfill the goal?

The evaluator.py Implementation
This script runs a list of test cases, catches the output, and prints a summary.
evaluator.py
29 Jun, 3:48 pm

Thanks for generating the scripts. I am getting the ModuleNotFoundError while running python run.py. Could you please guide me to solve this issue.

(.venv) PS C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home> python run.py

Traceback (most recent call last):

  File "C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home\run.py", line 2, in <module>

    from main import run_agent

  File "C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home\main.py", line 3, in <module>

    from prompts import SYSTEM_PROMPT

ModuleNotFoundError: No module named 'prompts'

(.venv) PS C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home>

The error ModuleNotFoundError: No module named 'prompts' means that Python cannot find the prompts.py file in the directory where your main.py is looking.

Here is how to fix this:

1. Verify File Placement
Ensure that prompts.py is in the exact same folder as main.py. Based on your path C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home\, your directory should look like this:

Plaintext
wolters_kluwer_agent_take_home/
├── .env
├── main.py
├── prompts.py    <-- Make sure this file exists here
├── tools.py
├── run.py
└── requirements.txt
2. Check for File Name Typos
Ensure the file is named exactly prompts.py (all lowercase, no extra spaces). If you named it Prompts.py or prompts.py.txt, Python will not find it.

3. Check for "Hidden" File Extensions
On Windows, sometimes files are named prompts.py.txt without you realizing it.

Open your folder in File Explorer.

Go to the View tab.

Check the box for File name extensions.

If you see .txt at the end of prompts.py, rename it to remove the .txt.

4. Fix a Potential Pathing Issue
If you are using PyCharm, sometimes it runs scripts from a slightly different working directory. To fix this, ensure prompts.py is inside the same folder as main.py and try running it directly from the terminal again:

Bash
cd C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home
python run.py
How to debug if it still fails:
Add this line to the very top of your main.py to see where Python is looking for files:

Python
import sys
print(sys.path)
If the path C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home is not in that list, you may need to right-click your wolters_kluwer_agent_take_home folder in PyCharm, select Mark Directory as, and choose Sources Root.

Does this resolve the import error?
29 Jun, 3:49 pm

Thanks, ModuleNotFoundError issue got solved, I am getting OpenAIError in running python run.py. Could you please guide me to solve this issue.

File "C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home\.venv\Lib\site-packages\openai\_client.py", line 230, in __init__

    raise OpenAIError(

openai.OpenAIError: Missing credentials. Please pass an `api_key`, `workload_identity`, `admin_api_key`, or set the `OPENAI_API_KEY` or `OPENAI_ADMIN_KEY` environment variable.

(.venv) PS C:\Users\prave\PycharmProjects\wolters_kluwer_agent_take_home>

The error openai.OpenAIError: Missing credentials means the openai library is not finding your API key. Even if your .env file exists, Python doesn't automatically "see" it unless you explicitly load it using the python-dotenv library.

The Fix
You need to add two lines to the very top of your main.py file to load your environment variables before the openai client is initialized.

Update the top of your main.py like this:

Python
import os
from dotenv import load_dotenv # Add this
load_dotenv()                 # Add this to load variables from .env

import openai
# ... rest of your imports
Why this happens
Environment Variables: When you define OPENAI_API_KEY in a file named .env, it is just a text file. The computer's operating system does not automatically pull those values into your running Python process.

load_dotenv(): This function reads the .env file and "injects" those key-value pairs into os.environ, which is what os.getenv("OPENAI_API_KEY") looks at.

Troubleshooting Checklist
If you still get the error after adding the lines above:

Check pip: Ensure python-dotenv is actually installed in your current virtual environment:

Bash
pip install python-dotenv
Case Sensitivity: Ensure your .env file is named exactly .env (it has no filename, only an extension). It should not be env.txt or .env.txt.

Verification: You can add a quick debug print before the client initialization to see if Python sees the key:

Python
print(f"DEBUG: Key found: {bool(os.getenv('OPENAI_API_KEY'))}")
client = openai.OpenAI(api_key=os.getenv("OPENAI_API_KEY"))
If it prints DEBUG: Key found: False, then the code is still not finding the .env file. Double-check that main.py and .env are in the exact same folder (wolters_kluwer_agent_take_home).

Once load_dotenv() runs, os.getenv will successfully retrieve your key, and the agent should start.

Does this resolve the import error?
29 Jun, 3:55 pm